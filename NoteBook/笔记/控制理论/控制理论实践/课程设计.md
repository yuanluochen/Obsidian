```matlab
clear all;
z_0 = [];
p_0 = [-1, -2, -10];
k_0 = 200;
sys_k_0 = zpk(z_0, p_0, k_0)
nyquist(sys_k_0)
sys_0 = feedback(sys_k_0, 1)
step(feedback(sys_k_0, 1))
[y, t] = step(sys_0);
ess = 1 - y;
ess1 = ess(length(ess)) % steady-state error
ymax = max(y); %calc top value;
mp = ((ymax -1) / 1 ) * 100 % overshoot
tp = spline(y, t, ymax) % top time


% calc correction sys
zeta = sqrt((log(0.163)^2 / (pi^2 + (log(0.163))^2)))
w_n = 4 / (8 * zeta)

num_exp = w_n^2;
den_exp = [1 2*zeta*w_n 0];
sys_exp = tf(num_exp, den_exp)

% calc control
sys_c = sys_exp * (1 / sys_0)
sys = sys_0 * sys_c;

[y, t] = step(feedback(sys, 1));
step(feedback(sys, 1))
mp = ((max(y) -1) / 1 ) * 100 % overshoot

margin(sys_k_0)
[gm, pm, wg, wp] = margin(sys_k_0)
margin(sys_c)
margin(sys)
[gm, pm, wg, wp] = margin(sys)

% chart step
step(feedback(sys, 1))
% chart rlocus
rlocus(sys)
% chart nyquist
nyquist(sys)


[num_0, den_0] = zp2tf(z_0, p_0, k_0);
sys_0 = tf(num_0, den_0)
[z_c, p_c, k_c] = zpkdata(sys_c, 'v');
[num_c, den_c] = zp2tf(z_c, p_c, k_c);
sys_c = tf(num_c, den_c)
[z, p, k] = zpkdata(sys, 'v');
[num, den] = zp2tf(z, p, k);
